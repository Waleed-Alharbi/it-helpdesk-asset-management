import { useEffect, useState } from 'react'; import { Bar, BarChart, Cell, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'; import { CheckCircle2, Clock3, TicketCheck } from 'lucide-react'; import { api } from '../api'; import { ErrorState, Loading } from '../components/States'
const colors = ['#4f46e5', '#14b8a6', '#f59e0b', '#ef4444', '#8b5cf6', '#38bdf8']; const Panel = ({ title, children }) => <article className="panel chart-panel"><div className="panel-head"><div><h3>{title}</h3><p>Service desk reporting period</p></div></div>{children}</article>
export default function Reports() { const [data, setData] = useState(null), [error, setError] = useState(''); const load = () => {setError(''); api.reports().then(setData).catch(e => setError(e.message))}; useEffect(load, []); if(error)return <ErrorState message={error} retry={load}/>; if(!data)return <Loading/>; return <><div className="page-heading"><div><p className="eyebrow">ANALYTICS & INSIGHTS</p><h1>Service desk reports</h1><p>Track support performance and the health of your IT asset estate.</p></div><button className="button secondary">Export report</button></div><div className="report-summary"><article><TicketCheck/><div><span>Total requests</span><strong>{data.open_vs_resolved.reduce((a,b)=>a+b.value,0)}</strong></div></article><article><CheckCircle2/><div><span>Resolution rate</span><strong>{data.resolution_rate}%</strong></div></article><article><Clock3/><div><span>Open workload</span><strong>{data.open_vs_resolved[0].value}</strong></div></article></div><section className="chart-grid reports"><Panel title="Monthly ticket trend"><ResponsiveContainer width="100%" height={270}><LineChart data={data.monthly_trend} margin={{left:-20,right:10}}><XAxis dataKey="name" axisLine={false} tickLine={false}/><YAxis axisLine={false} tickLine={false}/><Tooltip/><Line type="monotone" dataKey="value" stroke="#4f46e5" strokeWidth={3} dot={{r:4}}/></LineChart></ResponsiveContainer></Panel><Panel title="Open vs resolved"><ResponsiveContainer width="100%" height={270}><PieChart><Pie data={data.open_vs_resolved} dataKey="value" nameKey="name" innerRadius={70} outerRadius={100}>{data.open_vs_resolved.map((x,i)=><Cell key={x.name} fill={colors[i]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></Panel><Panel title="Tickets by department"><ResponsiveContainer width="100%" height={270}><BarChart data={data.tickets_by_department} margin={{left:-28}}><XAxis dataKey="name" tick={{fontSize:10}} axisLine={false} tickLine={false}/><YAxis axisLine={false} tickLine={false}/><Tooltip/><Bar dataKey="value" fill="#14b8a6" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></Panel><Panel title="Asset status"><ResponsiveContainer width="100%" height={270}><PieChart><Pie data={data.asset_status} dataKey="value" nameKey="name" innerRadius={65} outerRadius={100}>{data.asset_status.map((x,i)=><Cell key={x.name} fill={colors[i]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></Panel></section></> }
