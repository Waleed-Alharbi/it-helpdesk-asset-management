import { Check, Database, Moon, ShieldCheck, SlidersHorizontal } from 'lucide-react'; import { useState } from 'react'
export default function Settings({notify}){const[notices,setNotices]=useState({critical:true,assignment:true,daily:false}),[dark,setDark]=useState(false);const toggle=key=>setNotices({...notices,[key]:!notices[key]});return <><div className="page-heading"><div><p className="eyebrow">WORKSPACE PREFERENCES</p><h1>Settings</h1><p>Configure this portfolio demo environment and notification preferences.</p></div><button className="button primary" onClick={()=>notify('Settings saved for this session.')}><Check size={18}/>Save changes</button></div><div className="settings-grid"><section className="panel settings-card"><div className="settings-title"><Database/><div><h3>System information</h3><p>Details about the demo workspace.</p></div></div><dl><div><dt>Workspace</dt><dd>Northstar IT Operations</dd></div><div><dt>Environment</dt><dd><span className="badge active">Portfolio demo</span></dd></div><div><dt>API version</dt><dd>v1.0.0</dd></div><div><dt>Data source</dt><dd>SQLite · seeded locally</dd></div></dl></section><section className="panel settings-card"><div className="settings-title"><ShieldCheck/><div><h3>Notifications</h3><p>Choose which service desk updates you receive.</p></div></div>{[['critical','Critical incident alerts','Immediately notify when a critical ticket is created.'],['assignment','Ticket assignment updates','Receive an alert when a ticket is assigned to you.'],['daily','Daily operations summary','A concise daily summary of service desk activity.']].map(([key,title,copy])=><div className="setting-row" key={key}><div><strong>{title}</strong><span>{copy}</span></div><button className={`switch ${notices[key]?'on':''}`} onClick={()=>toggle(key)} aria-label={title}><i/></button></div>)}</section><section className="panel settings-card"><div className="settings-title"><Moon/><div><h3>Appearance</h3><p>Set your workspace visual preference.</p></div></div><div className="setting-row"><div><strong>Dark mode</strong><span>Currently this demo uses the enterprise light theme.</span></div><button className={`switch ${dark?'on':''}`} onClick={()=>setDark(!dark)} aria-label="Toggle dark mode"><i/></button></div><p className="hint">Theme preference is stored only for this browser session in the demo.</p></section><section className="panel settings-card"><div className="settings-title"><SlidersHorizontal/><div><h3>About this project</h3><p>An intentionally scoped MVP for an IT support portfolio.</p></div></div><p className="about-copy">It demonstrates ticket workflows, asset visibility, user management, reporting, a REST API, and persistent demo data without claiming production-readiness.</p><a className="text-button" href="http://localhost:8000/docs" target="_blank">Open API documentation ↗</a></section></div></>}
